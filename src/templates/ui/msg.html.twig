{%for item in msg %}
<div class="alert alert-dismissible alert-{{ item.status }}">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <div>{{ item.text }}</div>
</div>
{% endfor %}